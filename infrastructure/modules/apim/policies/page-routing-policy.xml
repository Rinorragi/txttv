<!--
    Page Routing Policy for TXT TV
    
    This policy handles the GET /page/{pageNumber} operation.
    It extracts the page number from the URL and serves the shared
    page-template fragment. Content is loaded dynamically via fetch()
    from the Content API (/content/{pageNumber}) at render time.
    
    Implementation: APIM policy-based HTML shell + client-side JSON loading
-->
<policies>
    <inbound>
        <base />
        
        <!-- Extract page number from route -->
        <set-variable name="pageNumber" value="@(context.Request.MatchedParameters.GetValueOrDefault("pageNumber"))" />

        <!-- Validate page number is a valid integer between 100-999 -->
        <choose>
            <when condition="@(
                !System.Text.RegularExpressions.Regex.IsMatch(
                    context.Variables.GetValueOrDefault<string>("pageNumber", "unknown"), @"^\d{3}$")
            )">
                <return-response>
                    <set-status code="400" reason="Bad Request" />
                    <set-header name="Content-Type" exists-action="override">
                        <value>application/json</value>
                    </set-header>
                    <set-body>@{
                        var pageNumber = context.Variables.GetValueOrDefault<string>("pageNumber", "unknown");
                        return new JObject(
                            new JProperty("error", "Invalid page number"),
                            new JProperty("message", "Page number must be an integer between 100 and 999"),
                            new JProperty("requestedValue", pageNumber)
                        ).ToString();
                    }</set-body>
                </return-response>
            </when>
        </choose>
        
        <!-- Log page view -->
        <trace source="TxtTV-PageRouting" severity="information">
            <message>@($"Page view: {context.Variables["pageNumber"]}")</message>
            <metadata name="pageNumber" value="@((string)context.Variables["pageNumber"])" />
        </trace>
        
        <!-- Serve shared page template (fragment includes return-response with body) -->
        <include-fragment fragment-id="page-template" />
    </inbound>
    
    <backend>
        <!-- No backend call - response is generated entirely by policy -->
    </backend>
    
    <outbound>
        <base />
    </outbound>
    
    <on-error>
        <base />
    </on-error>
</policies>
